export const INTERFACE_URL =
  window.REACT_APP_INTERFACE_URL || process.env.REACT_APP_INTERFACE_URL;

export const PUBLIC_PREFIX = '/api/';

// // 前端地址
export const LOCAL_URL = window.location.origin;
